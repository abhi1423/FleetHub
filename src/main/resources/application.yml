spring:
  application:
    name: FLEET-HUB
  datasource:
    url: jdbc:mysql://localhost:3306/GoodsCarrierDataBase?createDatabaseIfNotExist=true
    username: root
    password: Abhinav1423
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8085